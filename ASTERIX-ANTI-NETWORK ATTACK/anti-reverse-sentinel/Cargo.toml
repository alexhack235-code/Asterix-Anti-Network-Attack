[package]
name = "anti-reverse-sentinel"
version = "1.0.0"
edition = "2021"
authors = ["ASTERIX OS Project"]
description = "Pure-Rust Anti-Reverse Engineering, Anti-Debugging & Process Hardening Sentinel"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dependencies]
# Zero external runtime dependencies for maximum portability!